<% title t('.title') %>

<%= render 'form' %>

<div class="form-actions">
	<%= link_to t('app.buttons.show'), @live_event,
  :class => 'btn' %>
	<%= link_to t('app.buttons.list'), menu_live_events_path,
  :class => 'btn' %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>
