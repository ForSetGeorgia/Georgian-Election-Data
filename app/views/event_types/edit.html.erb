<% title t('.title') %>

<%= render 'form' %>

<div class="form-actions">
	<%= link_to t('app.buttons.show'), @event_type,
  :class => 'btn' %>
	<%= link_to t('app.buttons.list'), event_types_path,
  :class => 'btn' %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>

