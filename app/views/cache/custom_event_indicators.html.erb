<% title t('.title') %>

<% if request.post? %>
	<p>
		<%= t('.cleared') %>
	</p>
<% else %>
	<%= form_tag cache_custom_event_indicators_path do %>
		<p>
			<%= t('.explanation') %>
		</p>
		<p>
			<%= t('.time') %>
		</p>
		<p>
			<%= submit_tag t("app.buttons.create"), :id => "submit" %>
		</p>
	<% end %>
<% end %>

<div class="form-actions">
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>
