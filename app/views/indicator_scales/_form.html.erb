<%= semantic_form_for(@indicator_scale) do |f| %>
	<%= f.inputs do %>
		<%= f.input :indicator %>
		<%= f.input :min %>
		<%= f.input :max %>
	<% end %>

	<%= f.actions :submit, :reset, :cancel %>

<% end %>
