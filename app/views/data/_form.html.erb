<%= semantic_form_for(@datum) do |f| %>
	<%= f.inputs do %>
		<%= f.input :indicator %>
		<%= f.input :common_id %>
		<%= f.input :value %>
	<% end %>

	<%= f.actions :submit, :reset, :cancel %>
<% end %>
