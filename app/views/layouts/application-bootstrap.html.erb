<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? "#{yield(:title)} > #{t('app.common.app_name')}" :
				t('app.common.app_name') %></title>
    <%= include_gon %>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%# favicon_link_tag 'images/apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%# favicon_link_tag 'images/apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%# favicon_link_tag 'images/apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%# favicon_link_tag 'images/apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%# favicon_link_tag 'images/favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>
		<div id="wrapper">
      <div class="navbar navbar-fixed-top">
        <!-- Navbar header -->
        <div class="navbar-inner navbar-inner-head">
          <div class="container-fluid">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </a>
            <%= link_to(image_tag("header-text.png"), root_path(:locale => I18n.locale),
  								:title=> t('.title'), :class=>"brand")%>

            <ul class="nav pull-right">
  						<% I18n.available_locales.each do |locale| %>
  							<% if locale != I18n.locale %>
  								<li><%= link_to t("app.language.#{locale}"), params.merge(:locale => locale) %></li>
  							<% end %>
  						<% end %>
            </ul>
            <div class="nav-collapse">
              <ul class="nav pull-right">
                <% if user_signed_in? %>
                <li>
     		         <%= current_user.email %>
  							</li>
                <li>
     		         <%= link_to t('.admin'), admin_path %>
  							</li>
                <li>
     		         <%= link_to t('.sign_out'), destroy_user_session_path, :method => :delete %>
  							</li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>

        <!-- Navbar footer -->
        <div class="navbar-inner navbar-inner-bottom">
           <div class="container-fluid clear">
              <div class="navmenu" id="navmenu">
                 <p class="link-text">
                    <%= t('.header') %>
                    <span class="arrow_shape">&#x25BC;</span>
                 </p>
              </div>
              <div class="navmenu-select">
                 <p>
                    <%= @event_name %>
                 </p>
              </div>
           </div>
        </div>
        <%= render :partial => "layouts/event_menu_tabs" %>
      </div>

			<div id="main" class="container-fluid">
		    <div class="content">
		      <div class="row-fluid">
		         <div class="span12">
								<% flash.each do |key, msg| %>
									<div class="alert <%= flash_translation(key) %> fade in">
										<a href="#" data-dismiss="alert" class="close">×</a>
										<%= msg %>
									</div>
								<% end %>

								<% if !(params[:controller] == "root" && params[:action] == "index") %>
                   <h1><%= yield(:title) %></h1>
                <% end %>

								<%= yield %>
		          </div>
		      </div><!--/row-->
		    </div><!--/content-->
	    </div> <!-- /container -->
      <div class="push"><!--//--></div>
    </div><!--/wrapper-->


    <footer class="container-fluid">
		  <div class="row-fluid">
				<div class="ndi-logo span2">
					<a href="http://www.ndi.org" target="_blank">
						<img src="/assets/ndi-logo.png"/>
					</a>
				</div>
				<div class="footer-nav span8">
					<p>
						<%= link_to t('.about'), view_pages_path(:name => "about", :layout => "fancybox"), :class => "fancybox" %>
						<a>|</a>
						<%= link_to t('.data_source'), view_pages_path(:name => "data_source", :layout => "fancybox"), :class => "fancybox" %>
						<a>|</a>
						<%= link_to t('.contact'), contact_path %>
					</p>
				</div>
       <div class="js-logo span2">
				 <p><%= t(".js_link_text") %>:</p>
				 <a href="http://www.jumpstart.ge" target="_blank">
						 <img src="/assets/jumpstart-logo.png"/>
				 </a>
				</div>
	    </div><!--/row-->
    </footer>



  <%= javascript_include_tag "map" %>
  <%= javascript_tag do %>
		I18n.defaultLocale = "<%= I18n.default_locale %>";
		I18n.locale = "<%= I18n.locale %>";

		if (gon.openlayers){
			window.onload = map_init;
		}
	<% end %>

	<% if Rails.env.production? %>
		<!-- google analytics -->
		<script type="text/javascript">
			var _gaq = _gaq || [];
			_gaq.push(['_setAccount', 'UA-12801815-7']);
			_gaq.push(['_trackPageview']);
			(function() {
				var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
				ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
				var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
			})();
		</script>
	<% elsif Rails.env.staging? %>
		<!-- google analytics -->
		<script type="text/javascript">
			var _gaq = _gaq || [];
			_gaq.push(['_setAccount', 'UA-12801815-8']);
			_gaq.push(['_trackPageview']);
			(function() {
				var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
				ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
				var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
			})();
		</script>
	<% end %>
  </body>
</html>
