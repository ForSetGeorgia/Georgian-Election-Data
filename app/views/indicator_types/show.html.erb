<% title t('.title') %>

<p>
  <b><%= t('app.common.name') %>:</b>
  <%= @indicator_type.name %>
</p>

<p>
  <b><%= t('app.common.has_summary') %>:</b>
  <%= @indicator_type.has_summary %>
</p>

<p>
  <b><%= t('app.common.sort_order') %>:</b>
  <%= @indicator_type.sort_order %>
</p>

<div class="form-actions">
	<%= link_to t('app.buttons.edit'), edit_indicator_type_path(@indicator_type),
  :class => 'btn btn-primary' %>
	<%= link_to t('app.buttons.list'), indicator_types_path,
  :class => 'btn' %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>
