<% title t('.title') %>

<div class="form-actions">
	<%= link_to t('app.buttons.list'), admin_data_sets_path,
  :class => 'btn' %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>

<%= render 'form' %>


