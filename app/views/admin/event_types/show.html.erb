<% title t('.title') %>

<div class="form-actions">
	<%= link_to t('app.buttons.edit'), edit_admin_event_type_path(@event_type),
  :class => 'btn btn-primary' %>
	<%= link_to t('app.buttons.list'), admin_event_types_path,
  :class => 'btn' %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>

<p>
  <strong><%= t('app.common.name') %>:</strong>
  <%= @event_type.name %>
</p>
<p>
  <strong><%= t('app.common.sort_order') %>:</strong>
  <%= @event_type.sort_order %>
</p>


