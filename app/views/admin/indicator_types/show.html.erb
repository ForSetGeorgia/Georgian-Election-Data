<% title t('.title') %>

<div class="form-actions">
	<%= link_to t('app.buttons.edit'), edit_admin_indicator_type_path(@indicator_type),
  :class => 'btn btn-primary' %>
	<%= link_to t('app.buttons.list'), admin_indicator_types_path,
  :class => 'btn' %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>

<p>
  <strong><%= t('app.common.name') %>:</strong>
  <%= @indicator_type.name %>
</p>

<p>
  <strong><%= t('app.common.has_summary') %>:</strong>
  <%= @indicator_type.has_summary %>
</p>

<p>
  <strong><%= t('app.common.sort_order') %>:</strong>
  <%= @indicator_type.sort_order %>
</p>

