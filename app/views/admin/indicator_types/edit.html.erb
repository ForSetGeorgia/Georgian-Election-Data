<% title t('.title') %>

<div class="form-actions">
	<%= link_to t('app.buttons.show'), admin_indicator_type_path(@indicator_type),
  :class => 'btn' %>
	<%= link_to t('app.buttons.list'), admin_indicator_types_path,
  :class => 'btn' %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>

<%= render 'form' %>


