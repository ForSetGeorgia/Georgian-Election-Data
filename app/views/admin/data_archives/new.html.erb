<% title t('.title') %>

<div class="form-actions">
	<%= link_to t('app.buttons.list'), admin_data_archives_path, :class => "btn" %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>

<%= form_tag admin_data_archives_new_path do %>
	<p>
		<%= t('.explanation') %>
	</p>
	<p>
		<%= t('.size') %>
	</p>
	<p>
		<%= t('.time') %>
	</p>
	<p>
		<%= submit_tag t("app.buttons.create"), :id => "submit" %>
	</p>
<% end %>


