<% title t('.title') %>

<%= render 'form' %>

<div class="admin_nav">
	<%= link_to t('app.buttons.list'), event_indicator_relationships_path %>
	<%= link_to t('app.buttons.admin'), admin_path %>
</div>
