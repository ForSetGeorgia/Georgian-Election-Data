<% title t('.title') %>

<%= render 'form' %>

<div class="form-actions">
	<%= link_to t('app.buttons.show'), @core_indicator,
  :class => 'btn' %>
	<%= link_to t('app.buttons.list'), core_indicators_path,
  :class => 'btn' %>
	<%= link_to t('app.buttons.admin'), admin_path,
  :class => 'btn' %>
</div>


