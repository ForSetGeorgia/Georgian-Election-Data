<% title t('.title') %>

<table>
  <tr>
    <th><%= t('app.common.language')%></th>
    <th><%= t('app.common.name')%></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @locales.each do |locale| %>
  <tr>
    <td><%= locale.language %></td>
    <td><%= locale.name %></td>
    <td><%= link_to t('app.buttons.show'), locale %></td>
    <td><%= link_to t('app.buttons.edit'), edit_locale_path(locale) %></td>
    <td><%= link_to t('app.buttons.delete'), locale, :confirm => t('app.msgs.delete_confirm'), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('app.buttons.new_locales'), new_locale_path %>
